name: Release

on:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]

permissions:
  contents: write
  id-token: write
  attestations: write
  artifact-metadata: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    if: >-
      ${{
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.head_repository.full_name == github.repository
      }}
    uses: open-ships/ci/.github/workflows/release-go.yaml@564eb8691843720c6d9eb99d79dba7f3b437c0bc
    with:
      commit-sha: ${{ github.event.workflow_run.head_sha }}
      project-name: teleop
      distribution: source
      go-version: 1.26.0
